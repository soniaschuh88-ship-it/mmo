
# TODO

## Docker rootless (host + repo)
- [x] Update Dockerfiles to run as non-root (better compatibility with Docker rootless).
- [ ] Enable Docker rootless mode on the host (produce/run commands).
- [ ] Verify compose still works (docker compose build/up) under rootless Docker.
- [ ] Add documentation for host-side verification (`docker info`, running `docker compose up`).


